export default defineAppConfig({
  ui: {
    colors: {
      primary: 'sky',
      secondary: 'yellow',
      neutral: 'slate',
    },
    button: {
      defaultVariants: { color: 'primary', variant: 'solid' },
    },
  },
})
